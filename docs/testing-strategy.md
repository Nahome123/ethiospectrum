# Testing strategy

## ETH-028 billing verification

Unit tests cover interval validation, strict server-side Price resolution, provider-to-entitlement and safe-invoice mapping, owner Checkout/Portal authority, administrator reconciliation, generic localized failure mapping, the raw-body signature boundary, allowlisted events, duplicate/busy/failure transitions, authoritative refetch, localization alignment, async-only Server Actions, absence of Stripe or secrets in Client Components, and ETH-027/ETH-029 guards. Static architecture cases ensure no `NEXT_PUBLIC_STRIPE_*`, browser Price trust, raw payload persistence, card fields, or email/SMS delivery enters the slice.

`supabase/tests/database/stripe_subscriptions.test.sql` exercises the five tables and their constraints, forced RLS, all household permissions, platform administrator, specialist/content-editor/anonymous/cross-household denial, safe RPC projections, service-only synchronization, unique provider and event IDs, duplicate processing, bounded retry state, immutable audit history, active-only entitlement, cancellation and payment-failure revocation, out-of-order provider timestamps, fixed search paths, narrow grants, and the continued ETH-027 and ETH-029 boundaries. The global ETH-025/ETH-026/ETH-027 absence guards are narrowed to their own implementation files rather than deleting the regression checks.

Local Playwright coverage in `tests/e2e/billing.local.spec.ts` runs with `pnpm test:e2e:billing:local` against local Supabase and synthetic provider-state fixtures only. It covers logged-out/specialist/editor denial, owner monthly/annual form semantics without contacting Stripe, redirect non-authority, active/payment-failed/cancelled rendering, scheduled cancellation, Customer Portal form semantics, safe receipt links, household administrator/member/viewer projections, cross-household isolation, safe platform administration, English/Amharic/Spanish layouts, keyboard submission, narrow viewport overflow, and automated serious/critical axe checks. Provider calls, signed sandbox forwarding, Checkout completion, and Portal behavior require separate Stripe test-mode validation; automated tests never create live charges.

## ETH-027 appointment verification

`supabase/tests/database/appointment_scheduling.test.sql` adds 116 synthetic assertions: table and column reuse, the removed `appointments_access` policy, forced RLS, proof that no appointment policy or function references `can_access_household` or `household_specialists`, and the one-active-appointment index. It covers proposal authority, with household users, the content editor, and an unassigned specialist all denied, an active household-wide row granting nothing, and the full validation matrix: invalid timezone, nonexistent spring-forward time, ambiguous fall-back time, unsupported duration and modality, sub-24-hour and beyond-90-day proposals, video without a link, non-HTTPS and `javascript:` links, and a phone appointment carrying a link. It then verifies derived household and specialist identity, idempotent proposals, one live appointment per request, specialist and household overlap denial with back-to-back allowed, the read matrix across owner, viewer, outsider, administrator, assigned and unassigned specialist, consent rules including viewer denial, specialist self-accept denial, unacknowledged denial, stale-version denial, server-derived actor, timestamp and copy version, version increment, audit event, and re-accept denial, browser and elevated write denial, audit immutability, absence of any URL in audit metadata, completion limited to the specialist after the start time, decline and reschedule-with-supersede, automatic cancellation on assignment revocation and on request close with their reasons and immediate loss of specialist access, audit visibility, fixed search paths and narrow grants, and the deliberate absence of recurrence, external calendars, notifications, and ETH-028 billing.

Unit coverage exercises the proposal and consent schemas, meeting-URL scheme rejection, allowlists, blank-version rejection, timezone validation, Temporal-based daylight-saving resolution and its error codes, lead-time and horizon bounds, half-open overlap, the lifecycle map, the full permission matrix, Server Action error mapping for every controlled code, locale-preserving revalidation of all six surfaces, exclusion of browser-supplied consent actor, timestamp and copy version, the async-only `"use server"` boundary, client components free of elevated clients, and the no-recurrence, no-calendar, no-notification, and no-billing guards. The ETH-025 and ETH-026 guards that previously required ETH-027 to be absent are replaced with guards confirming those layers do not schedule appointments themselves. Local Playwright coverage in `tests/e2e/appointments.local.spec.ts` runs with `pnpm test:e2e:appointments:local` against a local Supabase stack using synthetic users only.

## ETH-026 specialist assignment verification

`supabase/tests/database/specialist_assignment.test.sql` adds 117 synthetic assertions: table and column reuse, assignment metadata, the audit table, author-kind backfill, constraints, indexes, forced RLS, anonymous denial, and assignment denial for household owners, household administrators, members, viewers, specialists (including self-assignment), and content editors. It covers platform-administrator assignment, ineligible-specialist denial, closed and cancelled denial, duplicate-assignment denial, one active specialist per request, one specialist across multiple requests, assignment-version concurrency, administrator revocation, revocation clearing the assignment, and immediate loss of thread read, message read, message write, and workload visibility. It also verifies assigned-specialist scope and its exclusion from other requests, other households, dependents, documents, roadmap items, reminders, household membership, and assignment history; specialist responses with server-derived author and author kind; idempotent responses; specialist inability to close, cancel, or revoke; automatic revocation on close and cancel with their audit reasons; audit immutability and administrator-only visibility; browser and elevated forgery denial for assignment, version, author kind, and audit rows; fixed search paths and narrow grants; proof that an active `household_specialists` row grants nothing and that neither it nor `can_access_household()` appears in any ETH-026 function or policy; plus ETH-025 regression and ETH-027 absence.

Unit coverage exercises assignment and response schemas, the permission matrix, lifecycle transitions and revocation-reason mapping, eligibility, blank-version rejection, error mapping for stale, duplicate, lifecycle, cap, and generic failures, locale-preserving revalidation of every affected surface, browser-identity exclusion, the async-only `"use server"` boundary, client components free of elevated clients, absence of content logging, and the request-level and no-appointment guards. The ETH-025 guard that previously required ETH-026 functions to be absent is replaced by guards confirming ETH-025 grants no specialist access itself, ETH-026 is request-level, and ETH-027 appointment behavior is absent. Local Playwright coverage in `tests/e2e/specialists.local.spec.ts` runs with `pnpm test:e2e:specialists:local` against a local Supabase stack and uses synthetic users only: logged-out and household-user denial, content-editor denial, the directory, assignment, household visibility of the assigned name and specialist response, specialist workload and response, absence of lifecycle and appointment controls, unreachable household areas, stale-version conflict, revocation with immediate list and direct-route denial, preserved historical messages, automatic revocation on close with its audit reason, the dormant household-wide row granting nothing, all three locales, keyboard operation, and narrow-viewport checks. One deliberate gap: the two-administrator stale-assignment-version conflict is covered by pgTAP and the Server Action unit tests but is not asserted in the browser, because the second administrator's Server Action request was never dispatched against the Next dev server and the scenario could not be reproduced reliably. Verify that conflict manually before release.

## ETH-025 support request verification

The synthetic pgTAP suite in `supabase/tests/database/support_requests.test.sql` verifies that the dormant thread/message tables are reused with the required columns and audit table; forced RLS; anonymous denial; owner/administrator/member/viewer reads; removed-member and cross-household denial; owner/administrator/member creation with derived identity; viewer write denial; specialist and actively-assigned-specialist denial; platform-administrator read access with creation/message/close write denial; subject/description/follow-up bounds; category and language allowlists; required server-timestamped acknowledgment; idempotent request and message creation; the five-open-request and 50-message caps; requester/owner/administrator close and cancel with member cross-close denial; message denial on closed and cancelled requests; reopen denial for browser and elevated writes; message/audit immutability; hard-delete denial; browser identity/household/status/specialist forgery denial; stale-version rejection; fixed function search paths and narrow grants; no dependent/document linkage; no ETH-026 assignment behavior; and ETH-008/ETH-009 regression checks.

Unit coverage exercises the request/message schemas and bounds, category/language allowlists, acknowledgment requirement, lifecycle transition and permission-matrix helpers, query normalization, Server Action error mapping (validation, caps, closed-state, stale, generic), locale-preserving revalidation, browser-identity exclusion, the async-only `"use server"` boundary, policy replacement away from specialist-inclusive helpers, and client components free of elevated clients. The former ETH-024 guard that asserted no ETH-025 code exists is replaced by guards confirming ETH-024 contains no support-request behavior and ETH-025 contains no ETH-026 assignment behavior. Local Playwright coverage in `tests/e2e/support.local.spec.ts` uses synthetic users only and runs with `pnpm test:e2e:support:local` against a local Supabase stack: it covers logged-out denial, the acknowledgment-gated creation flow with visible disclaimer and household-visibility notice, validation errors, list/detail/refresh/back navigation, follow-up messaging, confirmed close and cancel including a concurrent-tab conflict, member/viewer/cross-household role behavior, read-only administrator triage, specialist denial, absent assignment/attachment/emergency controls, Amharic and Spanish rendering, and a narrow-viewport overflow check. Open-request/message caps and exhaustive forgery cases remain pgTAP-level coverage.

## Member resource discovery verification

Synthetic pgTAP coverage verifies additive metadata constraints, forced bookmark RLS, authenticated catalog/detail access, approved locale selection and English fallback, bounded filters, private bookmark isolation, published-only mutations, assigned and featured selection, idempotent roadmap creation/restoration, viewer denial, administrator-only curation, optimistic conflicts, and anonymous denial. Unit tests cover query normalization, controlled Server Action arguments, safe error mapping, cache invalidation, and truthful hub navigation. Local Playwright coverage should verify searching/filtering, bookmark persistence, localized detail fallback, idempotent roadmap linking, administrator curation, keyboard operation, narrow layouts, and 200% zoom before release; it must use synthetic local Supabase data only.

## ETH-024 translation verification

ETH-024 unit coverage validates mutation locales, Unicode content, trimming, bounds, rejection notes, and reader locales. Database validation must exercise the translation lifecycle, stale source versions, different-user review, RLS/public-payload boundaries, audit immutability, and household isolation with synthetic users. Browser validation must cover approved localized selection, English fallback notices, stale hiding, editor authorization, keyboard workflow, and safe Markdown. A local Supabase failure before migrations is an infrastructure blocker, not a passing or failing database result.

## ETH-022 reminder verification

Reminder unit tests cover offset scheduling, timezone and daylight-saving rejection, five-minute minimums, rescheduling preparation, and capped unseen-count presentation. Database coverage is defined for recipient isolation, worker grants, immutable logs, optimistic schedule edits, lifecycle cancellation, retry and skip transitions, and roadmap due-date rescheduling. These tests require a completed local Supabase reset. The current local Supabase CLI/container schema-initialization blocker occurs before application migrations and pgTAP execution, so no ETH-022 migration or pgTAP result is claimed until that environment is repaired.

## ETH-021 roadmap verification

ETH-021 database tests use synthetic households and verify forced RLS, anonymous denial, owner/administrator/member/viewer boundaries, removed/unrelated denial, idempotent creation, member self-assignment restrictions, status transitions, completion/reopen behavior, stale writes, archive/restore, manual ordering, and no roadmap writes to reminders. Unit tests cover Unicode/whitespace/length/date validation, controlled query parsing, status rules, and the server-action/database-function boundary. Before rollout, run a local browser flow with synthetic accounts to verify all locales, keyboard ordering, read-only viewer behavior, archived-item visibility, narrow wrapping, and 200% zoom; do not use hosted data.

Unit tests cover branding, translation alignment, validation, rendering contracts, and the document binder's URL/query boundary. Playwright covers locale routing, root redirect, responsive navigation, accessibility scanning, protected redirects, and controlled local document flows using only synthetic data. Run `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e`, `pnpm test:a11y`, and `pnpm build` before review; run the mutation-capable `pnpm test:e2e:documents:local` only against a local Supabase stack.

Education-support coverage verifies the visitor/onboarding/ready landing-state mapping, English and Amharic copy, real guide/assistant/pricing destinations, keyboard-reachable action links, and the absence of household data or analytics payloads in the marketing section. Browser coverage checks the localized landing heading, guide and assistant links, keyboard focus, and narrow 320px presentation. It uses no child, school, health, or household fixture data.

Database authorization is tested locally with pgTAP under `supabase/tests/database/`. The suite creates only synthetic Auth users inside a transaction and rolls back its data. It verifies Auth-trigger synchronization, default roles, grants, RLS isolation, atomic household creation, permission escalation denial, and immediate revocation. ETH-011 additionally covers owner/administrator writes, member/viewer read-only access, cross-household denial, archive visibility, household reassignment denial, and immediate revocation. ETH-012 adds checks for the private `family-documents` bucket, required upload columns and constraints, anonymous denial, owner/administrator/member preparation, viewer denial, cross-household and removed-member denial, dependent-household matching, immutable household/path metadata, authorized archive, active-query exclusion, and exact-path Storage policy isolation. ETH-014 adds local checks for marker-protected state transitions, queue idempotency, worker-only claim/complete/fail execution, lease ownership, bounded retries, archive cancellation, safe failure persistence, and parent-document derivative RLS. Never point these commands at a hosted project.

ETH-015 database coverage must add the dedicated summary table, forced RLS, anonymous denial, active member/viewer reads, owner/administrator/member request access, viewer request denial, removal/cross-household/archive denial, incomplete/needs-OCR denial, duplicate prevention per document/language, atomic claim/attempt bounds, completed-result reuse, source-reference parent-document scoping, and the absence of normal-client write/worker grants. Use transactional synthetic fixtures only.

For document changes, reset and test the local database before application checks:

```bash
pnpm db:start
pnpm exec supabase db reset
pnpm exec supabase test db
pnpm exec supabase db lint --level error
pnpm db:types
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test
pnpm test:e2e
pnpm test:e2e:documents:local
pnpm build
```

Application unit tests do not require a live database. They verify that member display data uses profiles with an email fallback, administrator authorization reads `user_roles`, Supabase clients use the generated contract, no server key reaches browser-safe modules, and dependent validation normalizes allowed input. ETH-012 validation coverage includes PDF, DOCX, and TXT acceptance; unsupported MIME and extension rejection; empty and over-20-MiB file rejection; safe filename and trusted-path generation; dependent access; viewer upload denial; signed-upload preparation/completion/failure handling; archive and signed-download authorization; dashboard active counts; no administrative-client import; no public URL generation; and the Server Action export rule.

ETH-013 unit coverage verifies bounded metadata-search normalization, injection-shaped input handling, strict dates, page normalization, UUID-dependent and household-level filters, status allowlists, controlled sort mapping, URL preservation, page metadata, household scope, default archive exclusion, active-dependent verification, no `select('*')`, and no elevated/public-storage access. The query tests use fluent-client mocks and assert the server derives its household context rather than accepting one from the URL. ETH-014 unit coverage keeps parser input limits, no-secret/browser boundary, constant-time internal-route authorization, deterministic chunking, safe worker errors, retry-control eligibility, dashboard terminal-status counts, scheduler-origin validation, and Server Action export boundaries under test; the local pgTAP suite is the database authorization regression boundary.

ETH-015 unit and integration coverage must mock every provider call. It must validate language allowlists; request authorization; viewer denial; eligibility; idempotent reuse/retry; missing API key/model/worker-secret fail-closed behavior; constant-time worker-secret rejection; bounded worker batches; transient-versus-invalid provider failure behavior; structured-output limits; Unicode preservation; source IDs and excerpts; cross-document reference rejection; long-document batching/call limits; and prompt-injection resistance. It must also keep provider SDKs, API keys, document content, source excerpts, and provider payloads out of Client Components and logs. The automated Server Action export test must include each top-level `"use server"` module, not only the original processing action file.

ETH-016 database tests cover the dedicated OCR table/RLS, anonymous and browser-table denial, owner/administrator/member access, viewer/removed/unrelated denial, PDF/active-status eligibility, duplicate prevention, atomic claim, worker identity, bounded attempts, archive cancellation, page/chunk write denial, and household-scoped output. Unit tests mock every OCR provider and renderer call; they cover missing/incorrect worker secrets, incomplete provider configuration, malformed PDF headers, page count/dimension/pixel limits, private-path/file-size revalidation, provider timeout/failure mapping, empty output, Unicode normalization with Amharic and Spanish text, page/chunk ordering, derivative replacement arguments, and no public URL/logging/provider SDK in Client Components. Automated OCR tests never call a paid service.

ETH-017 database tests cover the question table, forced RLS, anonymous/browser-write denial, owner/administrator/member request access, viewer/removed/unrelated denial, same-document/language duplicate protection, controlled retry, lease/claim ownership, invalid completion shapes, cross-document citation rejection, and archive cancellation. Unit tests use only mocked providers and cover question/language validation, Server Action export boundaries, generic errors, secret comparison, strict structured output, prompt-injection-shaped source text, bounded source selection, provider failure mapping, source-key resolution, and the absence of admin/provider/secret imports in document Client Components. Local Playwright coverage uses synthetic documents and service-only synthetic completion data to verify English/Amharic/Spanish rendering, keyboard submission and source disclosure, mobile wrapping, queued/answering/failed/completed states, and viewer read-only behavior. No automated test calls a paid provider or a hosted project.

ETH-018 database tests extend the synthetic summary fixture with forced-RLS evaluation/review records, anonymous/browser-write denial, viewer read-only access, member feedback-only access, owner/administrator decisions, immediate removal/cross-household/archive denial, one review per reviewer/summary, controlled ratings/categories, derived reviewer/household identity, and a tampered cross-document reference that deterministically fails grounding. Unit tests cover Unicode/whitespace/length validation, action input boundaries, role decisions, server-only UI boundaries, and no browser-supplied identity. Local Playwright coverage must use only a synthetic completed summary and no provider call to verify owner/administrator/member/viewer workflow, locales, keyboard use, duplicate prevention, and mobile wrapping.

ETH-019 database tests cover forced RLS on conversations/messages; anonymous, viewer, removed-member, and cross-household denial; owner/administrator/member creation and send; immutable document/household/conversation links; en/am/es validation; bounded nonblank input; one active assistant response; message and conversation idempotency; worker claim/complete/fail/retry; same-document citation validation; and archive cancellation. Unit tests mock the provider and cover multilingual input, strict output/fallback shapes, bounded same-conversation history, prior assistant context exclusion as evidence, safe action errors, structured non-excerpt citations, and server-only boundaries. Local Playwright coverage must use synthetic documents and service-only synthetic completion: create/reopen multiple conversations, refresh persistence, viewer read-only behavior, pending/failed/retry states, source labels without excerpts or links, all locales, keyboard flow, and narrow layout. Automated tests never call a paid provider.

ETH-020 database tests verify that `document_pages` and `document_chunks` cannot be enumerated by any browser role, while the owner-bound citation RPC resolves only an active same-household completed source. They cover malformed/out-of-range indexes, cross-household callers, removed members, and archive revocation returning one unavailable shape, bounded evidence output, and logical-section behavior for non-PDF sources. Unit tests cover strict owner/index parsing, safe browser citation normalization without chunks/excerpts, RPC response filtering, and the server-only action boundary. Local Playwright coverage must use only synthetic completed PDF, DOCX, and TXT records and verify deep-link selection, keyboard/focus behavior, sheet close/back behavior, mobile/zoom wrapping, PDF page navigation only after authorization, and the unavailable state without source/error leakage. Automated tests do not call a provider or a hosted project.

ETH-023 database tests must use synthetic global-role users and verify anonymous/member/specialist denial; administrator-only workflow access; no household-data grant; English-only public reads; draft/review/archive/audit denial; immutable audit rows; idempotent create; stale-version rejection; slug immutability after first publication; rejection-note bounds; each valid and invalid transition; and administrator self-approval. Unit tests cover resource schema Unicode/trim/limits, action server-only boundaries, safe Markdown handling, and translation-key alignment. Local Playwright coverage must use synthetic resources to verify the public catalog/detail, administrator lifecycle, keyboard operation, focus/error announcements, and wrapping at mobile width and 200% zoom. No provider or hosted project is used.

The dedicated local-only Playwright document flow uses a synthetic owner, household, dependent, viewer, and TXT files. It covers binder authentication/empty state, English/Amharic/Spanish render, metadata search, dependent and status filters, title sorting, clear filters, query-preserving pagination, detail back navigation, authorized download, archive behavior, keyboard-opened mobile filters, and the controlled processing path. The test verifies that the internal worker route rejects a request with no processing secret, then shows the initial status, queues a synthetic uploaded TXT document with keyboard input, confirms the queued status, invokes the local-only worker with a process-generated secret, and observes the completed status without reading extracted text. It also creates a local-only viewer membership and verifies that the viewer cannot see a processing control. Normal `pnpm test:e2e` leaves this mutation test skipped; use `pnpm test:e2e:documents:local` only after the local Supabase stack is running. Never weaken type checks, policies, or tests merely to make CI pass.

Run the controlled document browser workflow only with a local Supabase stack:

```bash
pnpm db:start
pnpm test:e2e:documents:local
```

That dedicated Playwright configuration derives its values from local `supabase status`, refuses any non-local API target, generates a process-only `DOCUMENT_PROCESSING_SECRET`, uses a separate local app port, and creates only synthetic test data. The normal `pnpm test:e2e` configuration leaves that mutation test skipped, so it remains non-mutating and must not be pointed at a hosted project.

ETH-015 Playwright coverage must remain local-only and use a mocked server-side provider response rather than a paid API. It must verify that an authorized completed document shows language-specific generation controls; a viewer sees no generation control but can read an accessible existing summary; ineligible documents explain the processing/OCR prerequisite; queued/generating/completed/failed states render honestly; source references are reachable by keyboard; and English, Amharic, and Spanish summary UI render. The mock boundary must be explicitly test-only and refuse a non-local configuration.

ETH-016 Playwright coverage is local-only and must use a mocked OCR provider/renderer, never a real credential. It must verify that a `needs_ocr` PDF shows Run OCR for owner/administrator/member but not viewer; keyboard activation and duplicate-submission prevention; queued, processing, completed, failed, and retry labels; archive denial; English, Amharic, and Spanish rendering; and the visible accuracy/verify-original notice. The browser must never receive a page image, OCR text, secret, provider response, signed page-image URL, or raw worker error.

Manual document checks use only a local Supabase project and small synthetic fixtures:

1. Start the local stack, reset migrations, set local environment values, run the app, then sign in and complete household onboarding.
2. Upload a small synthetic PDF, DOCX, and TXT file separately; verify each is shown as uploaded in the binder and detail page, appears in the dashboard count, and downloads through the authenticated signed route. Add a synthetic dependent and verify metadata search, dependent/household-level/category/MIME/lifecycle/processing/date filters, controlled sorting, clear filters, and pagination preserve URL state without exposing another household.
3. For a synthetic TXT document, request processing as an owner, administrator, and member; verify the queued/processing/completed status UX, safe retry behavior, dashboard counts, and that the page never displays extracted text, job errors, worker details, a signed URL, or a secret. Confirm that a viewer, removed member, unrelated household, archived record, and terminal result cannot queue work. Invoke the worker only through a local server-only secret and verify no-secret invocation is rejected.
4. Attempt an empty file, an unsupported extension or MIME type, and a file larger than 20 MiB; each must remain unsuccessful and must not be presented as uploaded.
5. Use synthetic accounts and local role changes to verify a viewer cannot upload, a removed member cannot read or download, and a second household cannot find the document or its derivatives.
6. Archive an uploaded document as an owner, administrator, and eligible original uploader; verify it leaves the default binder, appears only through the archive filter where existing RLS permits it, its active job is cancelled, its derivatives are inaccessible, and its object is not physically deleted by this issue. Check the mobile filter dialog with keyboard focus and request native Amharic and Spanish review before release.
7. After a synthetic document completes processing, request a summary as an owner, administrator, and member in each supported language. Verify that a viewer can read an existing accessible result but cannot request work; a removed member and an unrelated household can do neither; source references identify genuine page/section material; a bounded preview is keyboard accessible; retry is offered only for retryable failures; and partial coverage is disclosed. Test prompt-injection-shaped synthetic text and confirm it neither changes the required summary shape nor causes secret/tool/URL behavior. Use a mocked provider only.
8. Upload a small synthetic image-only PDF and run its normal extraction until it shows `needs_ocr`. With a mocked local OCR provider, verify Run OCR/Retry OCR, queued/processing/completed/failed states, the `needs_ocr → completed` transition only after page/chunk rows exist, bounded retry, and the localized warning that OCR may contain errors and should be checked against the original. Confirm viewers/removed/other-household users cannot request it, archive cancels it, no page image becomes public, and empty OCR stays `needs_ocr`.
9. For a synthetic processed document, submit a short question as an owner, administrator, and member, then complete it with a mocked local provider result whose citations point to genuine same-document source rows. Confirm viewers can read the result but cannot submit; removed and other-household users can do neither; missing/incorrect worker secrets receive no document details; duplicate questions reuse one bounded row; an archive cancels active work; source previews are keyboard accessible; and all three locales wrap at narrow and 200% zoom. Treat Amharic and Spanish Q&A wording as awaiting native-speaker review.

Do not upload personal, production, or sensitive documents during automated or manual tests.

RBT training has unit coverage for source-body preservation, all seven section mappings, flashcard Q4–Q15 content, glossary terms, source attribution, progress normalization, Server Action input/identity boundaries, keyboard answer reveal, and glossary filtering. Its pgTAP test verifies the fixed course key, normalized sections, anonymous denial, own-row access, cross-user isolation, direct user-ID assignment denial, invalid-section rejection, uniqueness, and the all-sections completion timestamp. The local browser flow creates only synthetic confirmed users; it exercises the protected route, direct section navigation, English/Amharic/Spanish rendering, keyboard flashcards, glossary search, mobile reflow, persisted progress, and a second user seeing no first-user progress.

Run that mutation-capable browser flow only against local Supabase:

```bash
pnpm db:start
pnpm test:e2e:training:local
```

Manual RBT checks: sign in with a synthetic account; confirm each direct route and the resume link; use Tab and Enter to reveal and hide an answer; search then clear the glossary; mark a section complete and reload; sign in as a different synthetic user and verify the prior progress is absent; inspect the narrow viewport for horizontal page overflow; and verify the educational, non-certification notice remains visible. Do not alter supplied lesson text during manual QA; log clinical, attribution, translation, or source anomalies in `docs/rbt-training-content-review.md` for review.
